= form_tag charges_path do
  %h4 Click below to upgrade account
  %script.stripe-button{ src: "https://checkout.stripe.com/checkout.js", data_key: "#{@stripe_btn_data[:key]}", data_amount: @stripe_btn_data[:amount], data_description: "#{@stripe_btn_data[:description]}"}
